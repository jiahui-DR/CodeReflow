# 核心依赖
GitPython>=3.1.0
python-gitlab>=3.0.0

# 可选依赖（用于系统监控）
psutil>=5.8.0       # 系统资源监控

# 开发和测试依赖
pytest>=6.2.0       # 单元测试
pytest-cov>=2.12.0  # 测试覆盖率
black>=21.0.0        # 代码格式化
flake8>=3.9.0        # 代码检查
mypy>=0.910          # 类型检查

# 构建依赖
setuptools>=60.0.0   # 包构建
wheel>=0.37.0        # Wheel包格式
